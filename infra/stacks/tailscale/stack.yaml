# Stack manifest: this file's presence makes the admin stack (infra/spacelift)
# hydrate this directory into a Spacelift stack. Must be a non-empty YAML map.
description: Tailnet configuration (ACL policy file)
autodeploy: false
labels: [op]
