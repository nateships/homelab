# The proxy terminates TLS; argocd-server must serve plain HTTP or it
# redirect-loops.
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd
data:
  server.insecure: "true"
