# Any OIDC login gets admin. The UI is reachable only through the
# tailscale ingress, and tsidp only issues tokens to tailnet members:
# every tailnet member is an ArgoCD admin.
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
data:
  policy.default: role:admin
