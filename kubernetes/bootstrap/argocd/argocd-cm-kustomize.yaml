# The app kustomizations inflate helm charts; this flag lets ArgoCD's
# kustomize run helm. CI renders with the same flag.
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
data:
  kustomize.buildOptions: --enable-helm
