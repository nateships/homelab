# Backs up config PVC data to R2 (kopia file-system backup). Media
# volumes opt out per pod; git and Omni cover everything else.
name: velero
namespace: velero
# The node agent mounts kubelet pod dirs (hostPath).
namespaceLabels:
  pod-security.kubernetes.io/enforce: privileged
labels:
  category: storage
