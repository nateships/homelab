name: tailscale-operator
namespace: tailscale
labels:
  category: network
# Tailscale proxy pods (egress/ingress) run privileged; Talos enforces
# baseline PSA, so the namespace opts up.
namespaceLabels:
  pod-security.kubernetes.io/enforce: privileged
  homelab/tier: edge
