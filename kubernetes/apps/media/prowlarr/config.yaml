# Indexer manager. Config only (no media mount); reachable at
# https://prowlarr.<tailnet>.ts.net through the tailscale operator.
name: prowlarr
namespace: prowlarr
url: https://prowlarr.tail34eda.ts.net # site-specific: tailnet name
namespaceLabels:
  homelab/tier: media
