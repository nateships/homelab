# The OIDC client secret needs the ESO CRDs, so it cannot live in the
# bootstrap kustomization that applies on a bare cluster.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secret.yaml
