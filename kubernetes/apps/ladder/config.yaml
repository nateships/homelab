# ladder: strips paywalls and clutter with per-site rulesets and
# archive fallback. Pure HTTP, no headless browser. Reachable at
# https://ladder.<tailnet>.ts.net.
name: ladder
namespace: ladder
url: https://ladder.tail34eda.ts.net
labels:
  category: media
namespaceLabels:
  homelab/tier: utility
